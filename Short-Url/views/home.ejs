<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
  </head>

  <body>
    <h1>Hey from Server</h1>

    <ul>
      <% urls.forEach(url => { %>
      <!-- EJS loop: iterate over the 'urls' array passed from the server.
           For each element (called 'url'), the following block will run -->

      <li><%= url.shortId %></li>
      <!-- Output the 'shortId' property of the current 'url' object as a list item.
             <%= %> means the value will be inserted and HTML-escaped -->

      <% }) %>
    </ul>
  </body>
</html>
